using System;
using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;
using UnityEngine.InputSystem;

/*
public class Manager : MonoBehaviour
{
    private static Dictionary<MyActions, InputActionReference> input_actions;
    
    private void Start()
    {
        input_actions = FindObjectOfType<InputManager>().getInputActionRefDict();
    }

    public InputActionReference getAction(MyActions action_name)
    {
        return input_actions[action_name];
    }
}
*/

